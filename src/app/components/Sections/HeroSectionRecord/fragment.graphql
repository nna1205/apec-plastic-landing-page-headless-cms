fragment HeroSection on HeroSectionRecord {
  sectionHeader {
    id
    title
    subtitle
  }
  callToActions {
    id
    label
    slug
    variant
  }
  image {
    id
    responsiveImage {
      ...DatoImage
    }
  }
}
