fragment HeroSection on HeroSectionRecord {
  sectionHeader {
    id
    title
    subtitle
  }
  callToActions {
    id
    label
    url
    variant
  }
  image {
    id
    responsiveImage {
      ...DatoImage
    }
  }
}
